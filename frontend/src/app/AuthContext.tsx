import { createContext,useContext,useState,type ReactNode } from 'react'; import type { Role,User } from '../types/brightboard'; type Auth={user:User|null;login:(role:Role)=>void;logout:()=>void};const C=createContext<Auth>(null!);export const AuthProvider=({children}:{children:ReactNode})=>{const [user,setUser]=useState<User|null>(null);return <C.Provider value={{user,login:role=>setUser({id:'demo',name:role+' User',role}),logout:()=>setUser(null)}}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
