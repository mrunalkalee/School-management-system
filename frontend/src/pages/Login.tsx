import { useAuth } from '../app/AuthContext'; import type { Role } from '../types/brightboard'; export const Login=()=>{const {login}=useAuth();return <main className="page-container"><h1>Welcome to BrightBoard</h1><p>Choose a demo role to continue.</p>{(['Admin','Teacher','Student','Parent'] as Role[]).map(r=><button className="button" onClick={()=>login(r)} key={r}>{r}</button>)}</main>;
