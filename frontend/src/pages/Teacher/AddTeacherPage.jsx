import{useNavigate}from'react-router-dom';import TeacherForm from'../../components/Teacher/TeacherForm';import{createTeacher}from'../../api/teacherService';import{toPayload}from'./teacherValues';export default function AddTeacherPage(){const n=useNavigate();return <section className="form-page"><button onClick={()=>n('/teachers')}>← Back to directory</button><h1>Add Teacher</h1><p>Create a faculty profile and assign classes.</p><TeacherForm initialValues={{}} submitLabel="Add Teacher" onSubmit={async v=>{await createTeacher(toPayload(v));n('/teachers')}}/></section>}
