export const toFormValues = (teacher = {}) => ({ name: `${teacher.firstName || ''} ${teacher.lastName || ''}`.trim(), employeeId: teacher.employeeId || '', email: teacher.email || '', subject: teacher.subjectsHandled?.join(', ') || '', classesAssigned: teacher.classesAssigned?.join(', ') || '', phone: teacher.phone || '', status: teacher.isActive === false ? 'On Leave' : 'Active', photo: teacher.profilePhotoUrl || '' })
export const toPayload = (values) => { const [firstName, ...lastName] = values.name.trim().split(/\s+/); return { firstName, lastName: lastName.join(' ') || firstName, employeeId: values.employeeId, email: values.email, subjectsHandled: values.subject.split(',').map((item) => item.trim()).filter(Boolean), classesAssigned: values.classesAssigned.split(',').map((item) => item.trim()).filter(Boolean), phone: values.phone, profilePhotoUrl: values.photo, isActive: values.status === 'Active' } }
