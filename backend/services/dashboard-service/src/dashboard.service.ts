import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { DashboardSnapshot, DashboardSnapshotDocument } from './dashboard.schema';
@Injectable() export class DashboardSnapshotService { constructor(@InjectModel(DashboardSnapshot.name) private readonly model: Model<DashboardSnapshotDocument>) {} create(data: Record<string, unknown>) { return this.model.create(data); } findAll() { return this.model.find().lean(); } async findOne(id: string) { const item = await this.model.findById(id).lean(); if (!item) throw new NotFoundException('DashboardSnapshot not found'); return item; } }
