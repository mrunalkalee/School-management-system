import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type DashboardSnapshotDocument=HydratedDocument<DashboardSnapshot>; @Schema({timestamps:true,collection:'dashboard'}) export class DashboardSnapshot{@Prop({required:true}) role!:string;@Prop() userId?:string;@Prop({type:Object,default:{}}) payload!:Record<string,unknown>;@Prop({default:Date.now}) generatedAt!:Date;} export const DashboardSnapshotSchema=SchemaFactory.createForClass(DashboardSnapshot);
