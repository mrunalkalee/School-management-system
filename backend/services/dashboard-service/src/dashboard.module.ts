import { Module } from '@nestjs/common'; import { MongooseModule } from '@nestjs/mongoose'; import { DashboardSnapshot, DashboardSnapshotSchema } from './dashboard.schema'; import { DashboardSnapshotService } from './dashboard.service'; import { DashboardSnapshotController } from './dashboard.controller';
@Module({ imports:[MongooseModule.forRoot(process.env.MONGODB_URI || 'mongodb://localhost:27017/brightboard-dashboard'), MongooseModule.forFeature([{name:DashboardSnapshot.name,schema:DashboardSnapshotSchema}])], controllers:[DashboardSnapshotController], providers:[DashboardSnapshotService] }) export class DashboardSnapshotModule {}
