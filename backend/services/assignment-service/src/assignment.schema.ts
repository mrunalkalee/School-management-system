import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type AssignmentDocument=HydratedDocument<Assignment>; @Schema({timestamps:true,collection:'assignments'}) export class Assignment{@Prop({required:true}) classId!:string;@Prop() subject!:string;@Prop() title!:string;@Prop() dueDate!:Date;@Prop() teacherId!:string;@Prop({type:[Object],default:[]}) submissions!:Array<{studentId:string;status:string}>;} export const AssignmentSchema=SchemaFactory.createForClass(Assignment);
