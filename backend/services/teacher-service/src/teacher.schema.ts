import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type TeacherDocument=HydratedDocument<Teacher>; @Schema({timestamps:true,collection:'teachers'}) export class Teacher{@Prop({required:true}) teacherId!:string;@Prop() firstName!:string;@Prop() lastName!:string;@Prop() department?:string;@Prop({type:[String],default:[]}) subjects!:string[];@Prop({default:"active"}) status!:string;} export const TeacherSchema=SchemaFactory.createForClass(Teacher);
