import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type StudentDocument=HydratedDocument<Student>; @Schema({timestamps:true,collection:'students'}) export class Student{@Prop({required:true}) studentId!:string;@Prop() firstName!:string;@Prop() lastName!:string;@Prop() classId!:string;@Prop() parentId?:string;@Prop({default:"active"}) status!:string;} export const StudentSchema=SchemaFactory.createForClass(Student);
