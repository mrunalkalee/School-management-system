import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type LeaveDocument=HydratedDocument<Leave>; @Schema({timestamps:true,collection:'leaves'}) export class Leave{@Prop({required:true}) userId!:string;@Prop() role!:string;@Prop() from!:Date;@Prop() to!:Date;@Prop() reason!:string;@Prop({default:"pending"}) status!:string;} export const LeaveSchema=SchemaFactory.createForClass(Leave);
