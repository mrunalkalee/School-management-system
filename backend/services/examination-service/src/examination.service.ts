import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { Examination, ExaminationDocument } from './examination.schema';
@Injectable() export class ExaminationService { constructor(@InjectModel(Examination.name) private readonly model: Model<ExaminationDocument>) {} create(data: Record<string, unknown>) { return this.model.create(data); } findAll() { return this.model.find().lean(); } async findOne(id: string) { const item = await this.model.findById(id).lean(); if (!item) throw new NotFoundException('Examination not found'); return item; } }
