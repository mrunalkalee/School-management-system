import { Module } from '@nestjs/common'; import { MongooseModule } from '@nestjs/mongoose'; import { Examination, ExaminationSchema } from './examination.schema'; import { ExaminationService } from './examination.service'; import { ExaminationController } from './examination.controller';
@Module({ imports:[MongooseModule.forRoot(process.env.MONGODB_URI || 'mongodb://localhost:27017/brightboard-examination'), MongooseModule.forFeature([{name:Examination.name,schema:ExaminationSchema}])], controllers:[ExaminationController], providers:[ExaminationService] }) export class ExaminationModule {}
