import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type ExaminationDocument=HydratedDocument<Examination>; @Schema({timestamps:true,collection:'examinations'}) export class Examination{@Prop({required:true}) classId!:string;@Prop() subject!:string;@Prop() title!:string;@Prop() date!:Date;@Prop({type:[Object],default:[]}) results!:Array<{studentId:string;score:number;grade:string}>;} export const ExaminationSchema=SchemaFactory.createForClass(Examination);
