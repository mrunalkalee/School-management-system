import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type FeeDocument=HydratedDocument<Fee>; @Schema({timestamps:true,collection:'fees'}) export class Fee{@Prop({required:true}) studentId!:string;@Prop() term!:string;@Prop() amount!:number;@Prop({default:0}) paid!:number;@Prop() dueDate?:Date;@Prop({default:"pending"}) status!:string;} export const FeeSchema=SchemaFactory.createForClass(Fee);
