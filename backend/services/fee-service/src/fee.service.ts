import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { Fee, FeeDocument } from './fee.schema';
@Injectable() export class FeeService { constructor(@InjectModel(Fee.name) private readonly model: Model<FeeDocument>) {} create(data: Record<string, unknown>) { return this.model.create(data); } findAll() { return this.model.find().lean(); } async findOne(id: string) { const item = await this.model.findById(id).lean(); if (!item) throw new NotFoundException('Fee not found'); return item; } }
