import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type LibraryItemDocument=HydratedDocument<LibraryItem>; @Schema({timestamps:true,collection:'library'}) export class LibraryItem{@Prop({required:true}) isbn!:string;@Prop() title!:string;@Prop() author!:string;@Prop() category?:string;@Prop({default:0}) copies!:number;@Prop({default:0}) available!:number;} export const LibraryItemSchema=SchemaFactory.createForClass(LibraryItem);
