import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { TransportRoute, TransportRouteDocument } from './transport.schema';
@Injectable() export class TransportRouteService { constructor(@InjectModel(TransportRoute.name) private readonly model: Model<TransportRouteDocument>) {} create(data: Record<string, unknown>) { return this.model.create(data); } findAll() { return this.model.find().lean(); } async findOne(id: string) { const item = await this.model.findById(id).lean(); if (!item) throw new NotFoundException('TransportRoute not found'); return item; } }
