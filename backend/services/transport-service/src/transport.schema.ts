import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type TransportRouteDocument=HydratedDocument<TransportRoute>; @Schema({timestamps:true,collection:'transport'}) export class TransportRoute{@Prop({required:true}) routeNo!:string;@Prop() name!:string;@Prop({type:[String],default:[]}) stops!:string[];@Prop() vehicleNo?:string;@Prop() driverName?:string;} export const TransportRouteSchema=SchemaFactory.createForClass(TransportRoute);
