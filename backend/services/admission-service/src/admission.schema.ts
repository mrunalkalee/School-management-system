import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type AdmissionDocument=HydratedDocument<Admission>; @Schema({timestamps:true,collection:'admissions'}) export class Admission{@Prop({required:true}) applicationNo!:string;@Prop() studentName!:string;@Prop() grade!:string;@Prop() guardianEmail!:string;@Prop({default:"submitted"}) status!:string;} export const AdmissionSchema=SchemaFactory.createForClass(Admission);
