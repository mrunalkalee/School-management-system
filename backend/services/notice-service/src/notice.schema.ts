import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type NoticeDocument=HydratedDocument<Notice>; @Schema({timestamps:true,collection:'notices'}) export class Notice{@Prop({required:true}) title!:string;@Prop() body!:string;@Prop({type:[String],default:[]}) audience!:string[];@Prop({default:Date.now}) publishedAt!:Date;@Prop() authorId?:string;} export const NoticeSchema=SchemaFactory.createForClass(Notice);
