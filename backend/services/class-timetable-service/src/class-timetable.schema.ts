import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type ClassTimetableDocument=HydratedDocument<ClassTimetable>; @Schema({timestamps:true,collection:'class-timetable'}) export class ClassTimetable{@Prop({required:true}) classId!:string;@Prop() section?:string;@Prop() day!:string;@Prop({type:[Object],default:[]}) periods!:Array<{subject:string;teacherId:string;start:string;end:string}>;} export const ClassTimetableSchema=SchemaFactory.createForClass(ClassTimetable);
