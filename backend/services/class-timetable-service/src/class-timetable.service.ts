import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { ClassTimetable, ClassTimetableDocument } from './class-timetable.schema';
@Injectable() export class ClassTimetableService { constructor(@InjectModel(ClassTimetable.name) private readonly model: Model<ClassTimetableDocument>) {} create(data: Record<string, unknown>) { return this.model.create(data); } findAll() { return this.model.find().lean(); } async findOne(id: string) { const item = await this.model.findById(id).lean(); if (!item) throw new NotFoundException('ClassTimetable not found'); return item; } }
