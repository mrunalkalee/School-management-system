import { Module } from '@nestjs/common'; import { MongooseModule } from '@nestjs/mongoose'; import { ClassTimetable, ClassTimetableSchema } from './class-timetable.schema'; import { ClassTimetableService } from './class-timetable.service'; import { ClassTimetableController } from './class-timetable.controller';
@Module({ imports:[MongooseModule.forRoot(process.env.MONGODB_URI || 'mongodb://localhost:27017/brightboard-class-timetable'), MongooseModule.forFeature([{name:ClassTimetable.name,schema:ClassTimetableSchema}])], controllers:[ClassTimetableController], providers:[ClassTimetableService] }) export class ClassTimetableModule {}
