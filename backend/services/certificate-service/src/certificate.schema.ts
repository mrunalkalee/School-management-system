import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type CertificateDocument=HydratedDocument<Certificate>; @Schema({timestamps:true,collection:'certificates'}) export class Certificate{@Prop({required:true}) studentId!:string;@Prop() type!:string;@Prop({default:Date.now}) issuedAt!:Date;@Prop() issuedBy?:string;@Prop() referenceNo!:string;} export const CertificateSchema=SchemaFactory.createForClass(Certificate);
