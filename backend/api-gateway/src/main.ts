import { Controller, Module, Req, Res, All, Injectable, CanActivate, ExecutionContext, UseGuards } from '@nestjs/common';
import { NestFactory } from '@nestjs/core'; import { HttpModule, HttpService } from '@nestjs/axios'; import { firstValueFrom } from 'rxjs';
const services:Record<string,string>={auth:process.env.AUTH_SERVICE_URL||'http://localhost:3001',students:process.env.STUDENT_SERVICE_URL||'http://localhost:3002',teachers:process.env.TEACHER_SERVICE_URL||'http://localhost:3003',admissions:process.env.ADMISSION_SERVICE_URL||'http://localhost:3004',attendance:process.env.ATTENDANCE_SERVICE_URL||'http://localhost:3006',assignments:process.env.ASSIGNMENT_SERVICE_URL||'http://localhost:3007',examinations:process.env.EXAMINATION_SERVICE_URL||'http://localhost:3008',fees:process.env.FEE_SERVICE_URL||'http://localhost:3009',notices:process.env.NOTICE_SERVICE_URL||'http://localhost:3014',dashboard:process.env.DASHBOARD_SERVICE_URL||'http://localhost:3016'};
@Injectable() class GatewayRoleGuard implements CanActivate {canActivate(c:ExecutionContext){const req=c.switchToHttp().getRequest();return req.path.startsWith('/auth')||Boolean(req.headers.authorization);}}
@UseGuards(GatewayRoleGuard) @Controller() class GatewayController {constructor(private http:HttpService){} @All(':service/*path') async proxy(@Req() req:any,@Res() res:any){const base=services[req.params.service];if(!base)return res.status(404).json({message:'Unknown service'});const suffix=req.params.path?.join('/')||'';const response=await firstValueFrom(this.http.request({method:req.method,url:base+'/'+suffix,data:req.body,params:req.query,headers:{authorization:req.headers.authorization||''},validateStatus:()=>true}));return res.status(response.status).send(response.data);}}
@Module({imports:[HttpModule],controllers:[GatewayController],providers:[GatewayRoleGuard]}) class AppModule{} async function bootstrap(){const app=await NestFactory.create(AppModule);app.enableCors();await app.listen(process.env.PORT||3000);}bootstrap();
