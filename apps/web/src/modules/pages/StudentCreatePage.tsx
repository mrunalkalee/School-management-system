import { useNavigate } from 'react-router-dom'; import { useState } from 'react'; import { StudentForm } from '../components/StudentForm'; import { createStudent } from '../services/students.service';
export function StudentCreatePage() { const navigate = useNavigate(); const [error, setError] = useState(''); const [submitting, setSubmitting] = useState(false); return <main className="student-form-page">{error && <p className="students-inline-error" role="alert">{error}</p>}<StudentForm submitting={submitting} onSubmit={async (data) => { setError(''); setSubmitting(true); try { const student = await createStudent(data); navigate(`/students/${student._id}`); } catch { setError('Could not create student. Check unique roll and admission numbers, then try again.'); } finally { setSubmitting(false); } }} /></main>; }
